@using Microsoft.AspNetCore.Identity
@model IEnumerable<IdentityRole>
@{
    ViewData["Title"] = "AllRoles";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}
    <h1>Roles</h1>
    <br />

    <div class="row mb-3">
        <div class="col-lg-4">
            <form method="post" asp-action="AddRole" asp-controller="ApplicationRole">
                <div class="input-group">
                    <input name="roleName" class="form-control w-25">
                    <span class="input-group-btn">
                        <button class="btn btn-info">Add New Role</button>
                    </span>
                </div>
            </form>
        </div>
    </div>

    
    <table class="table table-striped" id="roleTable">
        <thead>
            <tr>
                <th>
                    Role
                </th>
                <th>
                    Users
                </th>
                <th style="width:10%">
                    Actions
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var role in Model)
            {
                <tr>
                    <td>
                        @role.Name
                    </td>
                     <td>
                    @{

                        string  WelcomeMessage(string username)
                        {
                            return "Dear " + username ;

                        }


                    }

                    @{ WelcomeMessage("John");}x
                     </td>
                    <td class="text-right">
                        <div class="btn-group">
                            @*<button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Action
                            </button>
                            <div class="dropdown-menu shadow animated--grow-in">
                                <a class="dropdown-item">
                                    <i class="fas fa-pencil-alt"></i> Edit
                                </a>


                                
                                @if (role.Name == "SuperAdmin")
                                {
                                    <a style='margin-right:16px' asp-controller="ApplicationRole" asp-action="ListAllRoles" asp-route-roleId="@role.Id" class="dropdown-item">
                                        <i class="fas fa-wrench"></i> Manage Permissions
                                    </a>
                                }
                            </div>*@

                             <a class="btn btn-sm btn-primary" asp-controller="Permission" asp-action="Index" asp-route-roleId="@role.Id">Update</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>