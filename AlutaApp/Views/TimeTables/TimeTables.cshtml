@model IEnumerable<AlutaApp.Models.TimeTable>

@{
    ViewData["Title"] = "Trivias";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<div id="content" class="flex ">
                <!-- ############ Main START-->
                <div>
                    <div class="page-hero page-container " id="page-hero">
                        <div class="padding d-flex">
                            <div class="page-title">
                                <h4 class="text-md text-highlight">Time Tables</h4>
                                <small class="text-muted"></small>
                                <br />
                                <div>
                               @* @{
                            var models = new TGIFMatch();
                            await Html.RenderPartialAsync("_AddTGIFPartial", models);

                        }*@
                            </div>
                            </div>
                            
                            
                        </div>
                    </div>
       
        <div class="page-content page-container" id="page-content">
                        <div class="padding">
                <div class="table-responsive">@{

                        await Html.RenderPartialAsync("_TimeTableListPartial", Model);

                    }
                    
                                </div>
                                </div></div>
                               </div>
                                
                                </div>
                               

